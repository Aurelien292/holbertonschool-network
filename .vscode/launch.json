{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Flask",
            "type": "python",
            "request": "launch",
            "program": "holbertonschool-HbNb/test/aurelien/part3/run.py",  // Remplace par le fichier principal de ton application Flask
            "env": {
                "FLASK_APP": "run.py",  // Remplace par ton fichier Flask principal
                "FLASK_ENV": "development"
            },
            "jinja": true,
            "console": "integratedTerminal",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}